project:
  id: sanctions-exposure-index
  name: "Sanctions Exposure Index"
  description: "Public-data early warning for sanctions-expansion pressure."

news_query: "(sanctions OR embargo OR export controls OR asset freeze) when:7d"

sources:
  - name: "OFAC Sanctions List Service"
  - name: "OFAC press releases"
  - name: "FRED Brent/WTI"

refresh:
  interval_hours: 6
  retry_attempts: 5
